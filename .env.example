# Core (required)
BOT_TOKEN=YOUR_TELEGRAM_BOT_TOKEN

# If your Mongo password contains special characters (e.g. @, :, /), URL-encode them.
# Example: p@ssword -> p%40ssword
MONGO_URI=mongodb+srv://USERNAME:PASSWORD@cluster0.yifwyrd.mongodb.net/?appName=Cluster0

LOG_CHANNEL_ID=-100XXXXXXXXXX

# Super admin (required: one of these)
SUPER_ADMIN_IDS=123456789,987654321
# SUPER_ADMIN_ID=123456789

# Premium groups (set all 3 for category-based plans)
MOVIE_PREMIUM_GROUP_ID=-1001111111111
DESI_PREMIUM_GROUP_ID=-1002222222222
NON_DESI_PREMIUM_GROUP_ID=-1003333333333

# Optional fallback general group
# PREMIUM_GROUP_ID=-1004444444444

# Support — single external contact (Telegram @username or full https URL). Used by /support and buttons.
# Example: SUPPORT_CONTACT=@YourSupportBot
# Example: SUPPORT_CONTACT=https://t.me/YourSupportBot
SUPPORT_CONTACT=@YourSupportBot

# Runtime
NODE_ENV=production
PORT=3000

# HTTP /health (when PORT is set) — restrict in production
# HEALTH_CHECK_TOKEN=long-random-secret
# HEALTH_CHECK_ALLOWED_IPS=127.0.0.1,10.0.0.5
# Set true behind nginx to honor X-Forwarded-For
# TRUST_PROXY=true
# If both token and IPs are set: pass if token OR IP matches (set true to require BOTH)
# HEALTH_CHECK_REQUIRE_TOKEN_AND_IP=false

# Daily JSON backup to log channel: default strips user IDs and proof file references
# DAILY_BACKUP_INCLUDE_USER_IDS=false

# Optional DNS fallback for Mongo SRV lookup
MONGO_DNS_SERVERS=8.8.8.8,1.1.1.1

# Optional Mongo initial-connect retry tuning (helps with transient DNS issues)
MONGO_CONNECT_RETRY_BASE_MS=5000
MONGO_CONNECT_RETRY_MAX_MS=60000
MONGO_CONNECT_RETRY_JITTER_MS=1500

# Subscription/referral defaults
INVITE_LINK_TTL_MINUTES=10
# Delay for deleting bot/user flow messages (default 30 minutes)
MESSAGE_DELETE_DELAY_MINUTES=30
REFERRAL_REWARD_DISCOUNT_PERCENT=10
REJOINING_PENALTY=20

# Seller program
SELLER_COMMISSION_PERCENT=15
SELLER_MIN_WITHDRAW_REFERRALS=10
SELLER_MIN_WITHDRAW_BALANCE=200
# Minimum hours after request before super admin may approve (anti-rush). Use 0 for no wait.
SELLER_WITHDRAW_MIN_PROCESS_HOURS=24
# Set true to allow approving before the minimum wait (optional; default off).
SELLER_WITHDRAW_ALLOW_EARLY_APPROVAL=false
SELLER_PAYOUT_HISTORY_LIMIT=10

# Phase 3: anti-fraud + pending-request reminders
PAYMENT_PROOF_SPAM_WINDOW_MINUTES=10
PAYMENT_PROOF_MAX_ATTEMPTS=5
PAYMENT_PROOF_COOLDOWN_MINUTES=30
ADMIN_ACTION_CONFIRM_TTL_SECONDS=45
PENDING_REQUEST_REMINDER_AFTER_HOURS=2
PENDING_REQUEST_REMINDER_REPEAT_HOURS=12

# Retention cleanup policy
RETENTION_PENDING_REQUEST_DAYS=14
RETENTION_ADMIN_LOG_DAYS=90
RETENTION_DAILY_SUMMARY_DAYS=180
RETENTION_INVITE_METADATA_DAYS=7
RETENTION_LOG_FILES_DAYS=30

# Optional cron customizations
CRON_TIMEZONE=Asia/Kolkata
# REMINDER_CRON_SCHEDULES=0 8 * * *,0 20 * * *
# CLEANUP_CRON_SCHEDULE=20 3 * * *
CRON_LEADER_LOCK_ENABLED=true
CRON_LEADER_LOCK_TTL_SECONDS=120
# CRON_INSTANCE_ID=bot-instance-1
PROTECT_BOT_MESSAGES=false